/** @type {import('tailwindcss').Config} */
export default {
    darkMode: 'class', // You MUST define this if you're toggling dark mode with a class
    content: [
      "./index.html",
      "./src/**/*.{js,ts,jsx,tsx}"
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  }